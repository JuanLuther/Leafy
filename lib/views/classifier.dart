import 'package:flutter/material.dart';

class Classifier extends StatelessWidget {
  const Classifier({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("This is a Classifier");
  }
}
